Django==5.2.13
django-cors-headers==4.9.0
djangorestframework==3.17.1
djangorestframework_simplejwt==5.5.1
python-dotenv==1.2.2
uvicorn==0.25.0
pytest==9.0.3
requests==2.33.1
reportlab==4.2.2
psycopg==3.2.9
boto3==1.35.99
pgvector==0.3.6
WeasyPrint==62.3
PyJWT[crypto]==2.10.1
cryptography==44.0.0
exponent-server-sdk==2.2.0
psycopg[binary]==3.2.9
psycopg2-binary==2.9.10

# NOTE: embeddings/RAG/PCA/UMAP/clustering are intentionally NOT installed here.
# Per the "models are called via an API, not locally downloaded" requirement,
# the backend only ever reaches that functionality over HTTP through
# `vectors.ml_bridge` -> the dedicated embedding/ML service (`services/embedding`,
# itself a thin wrapper around the `machinelearning/` package -- the single
# source of truth). If that service is unreachable, `vectors.ml_bridge` and
# `api.llm_retrieval` degrade to "most recent contexts" rather than trying to
# run any model in-process here.
