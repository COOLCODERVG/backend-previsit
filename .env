DB_PASSWORD=)6#T$CIoa]a$4Fyid7K)1bB451ve
EXPO_PUBLIC_BACKEND_URL=http://127.0.0.1:8000/

# --- LLM inference transport (backend/api/llm_client.py) ---
# Leave both unset to no-op (falls back to template-based generation).
#
# Option A: self-hosted HTTP inference service (services/llm), used for local dev/testing.
# LLM_INFERENCE_URL=http://127.0.0.1:8080
#
# Option B: AWS SageMaker Serverless Inference (takes precedence over LLM_INFERENCE_URL when set).
# SAGEMAKER_ENDPOINT=neuravia-llama-3-2-1b-instruct
# AWS_REGION=us-east-1